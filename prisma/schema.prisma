generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model VisitorRequest {
  id              Int      @id @default(autoincrement())
  requestId       String   @unique @default(uuid())
  name            String
  citizenshipNo   String
  organization    String
  purpose         String
  personToMeet    String
  visitDate       DateTime
  phone           String
  email           String
  status          String   @default("PENDING") // PENDING, APPROVED, REJECTED, CHECKED_IN, CHECKED_OUT
  qrCode          String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
